---
phase: 03-auth-and-youtube-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/build.gradle.kts
  - local.properties
autonomous: false
requirements: [AUTH-01, AUTH-02, AUTH-03, YT-01, YT-04, YT-05]

user_setup:
  - service: google-cloud-console
    why: "YouTube Data API v3 access and OAuth 2.0 Android client"
    env_vars:
      - name: YOUTUBE_API_KEY
        source: "Google Cloud Console → APIs & Services → Credentials → API Keys"
      - name: GOOGLE_CLIENT_ID
        source: "Google Cloud Console → APIs & Services → Credentials → OAuth 2.0 Client IDs (type: Android)"
    dashboard_config:
      - task: "Enable YouTube Data API v3"
        location: "Google Cloud Console → APIs & Services → Library → search 'YouTube Data API v3' → Enable"
      - task: "Create OAuth 2.0 Android client ID"
        location: "Google Cloud Console → APIs & Services → Credentials → Create Credentials → OAuth client ID → Android. Set package name: com.musicali.app. Set SHA-1 fingerprint from your debug keystore (run: keytool -keystore ~/.android/debug.keystore -list -v, password: android). Note the Client ID value."
      - task: "Create API Key"
        location: "Google Cloud Console → APIs & Services → Credentials → Create Credentials → API Key. Copy the key value."
      - task: "Register OAuth redirect URI"
        location: "The redirect URI com.musicali.app:/oauth2redirect is automatically registered for Android OAuth clients by package name. No extra step needed for Android native apps."

must_haves:
  truths:
    - "Project builds cleanly with all Phase 3 dependencies added"
    - "BuildConfig.YOUTUBE_API_KEY is populated from local.properties"
    - "BuildConfig.GOOGLE_CLIENT_ID is populated from local.properties"
    - "The AppAuth redirect scheme manifest placeholder is set to com.musicali.app"
    - "KSP plugin version prefix matches Kotlin 2.3.20 (fixes Room + Hilt code generation with K2)"
  artifacts:
    - path: "app/build.gradle.kts"
      provides: "All Phase 3 dependencies + BuildConfig fields"
      contains: "net.openid:appauth:0.11.1"
    - path: "local.properties"
      provides: "Developer-local secrets (gitignored)"
      contains: "YOUTUBE_API_KEY="
  key_links:
    - from: "local.properties"
      to: "BuildConfig.YOUTUBE_API_KEY"
      via: "buildConfigField in app/build.gradle.kts"
      pattern: "buildConfigField.*YOUTUBE_API_KEY"
---

<objective>
Developer sets up Google Cloud Console credentials (human action), then the build is updated with all Phase 3 dependencies and BuildConfig fields so subsequent plans can compile and run.

Purpose: Phase 3 auth and YouTube integration requires two external credentials (API key + OAuth client ID) that only the developer can create. These must exist before any code can compile that references them. The build.gradle.kts changes in this plan are prerequisites for all other Phase 3 plans.

Output: Updated build.gradle.kts with AppAuth, security-crypto, Retrofit 3 BOM, Credential Manager deps, BuildConfig fields for YOUTUBE_API_KEY and GOOGLE_CLIENT_ID, and the AppAuth manifest placeholder. A local.properties file with developer-provided keys. Project compiles clean. KSP version aligned to Kotlin 2.3.20.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-auth-and-youtube-integration/03-CONTEXT.md
@.planning/phases/03-auth-and-youtube-integration/03-RESEARCH.md
</context>

<interfaces>
<!-- Existing NetworkModule pattern for reference -->
<!-- From app/src/main/java/com/musicali/app/di/NetworkModule.kt -->
```kotlin
@Module
@InstallIn(SingletonComponent::class)
object NetworkModule {
    @Provides @Singleton
    fun provideOkHttpClient(): OkHttpClient = OkHttpClient.Builder()
        .connectTimeout(15, TimeUnit.SECONDS)
        .readTimeout(30, TimeUnit.SECONDS)
        .build()
}
```

<!-- Current build.gradle.kts dependency block (Phase 1/2 state) -->
<!-- Notable: NO serialization plugin, NO Retrofit, NO AppAuth, NO security-crypto, NO Credential Manager -->
<!-- Hilt is already at 2.59.2, OkHttp at 4.12.0 -->
</interfaces>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Google Cloud Console setup</name>
  <read_first>
    - .planning/phases/03-auth-and-youtube-integration/03-RESEARCH.md (§Open Questions #2 — GCP setup steps)
    - local.properties (check if YOUTUBE_API_KEY already present)
  </read_first>
  <what-needs-doing>
    The developer must complete these steps in Google Cloud Console before Task 2 can proceed. Claude cannot do this via CLI.

    STEP 1 — Enable YouTube Data API v3:
      Go to: https://console.cloud.google.com/apis/library
      Search "YouTube Data API v3" → Click Enable

    STEP 2 — Create OAuth 2.0 Android client ID:
      Go to: APIs & Services → Credentials → Create Credentials → OAuth client ID
      Application type: Android
      Package name: com.musicali.app
      SHA-1 certificate fingerprint: run this command to get it:
        keytool -keystore ~/.android/debug.keystore -list -v -alias androiddebugkey
        (password: android)
      Copy the resulting "Client ID" value (format: NNNNNN-xxxxxxxx.apps.googleusercontent.com)

    STEP 3 — Create API Key:
      Go to: APIs & Services → Credentials → Create Credentials → API key
      Optionally restrict it to YouTube Data API v3
      Copy the key value

    STEP 4 — Add credentials to local.properties:
      Open local.properties in the project root (create it if missing — it is gitignored)
      Add these lines (replace with your actual values):
        YOUTUBE_API_KEY=AIzaXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX
        GOOGLE_CLIENT_ID=NNNNNN-xxxxxxxx.apps.googleusercontent.com
  </what-needs-doing>
  <resume-signal>Type "done" when all four steps are complete and local.properties has both keys, or describe any issues</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Add Phase 3 dependencies, BuildConfig fields, and align KSP version in build.gradle.kts</name>
  <read_first>
    - app/build.gradle.kts (current state — read before modifying; note the existing KSP plugin version)
    - local.properties (verify both keys are present before this task runs)
    - .planning/phases/03-auth-and-youtube-integration/03-RESEARCH.md (§Standard Stack — Installation block; §Open Questions — KSP version note)
  </read_first>
  <action>
    Make the following additions/changes to app/build.gradle.kts:

    0. FIX KSP plugin version — the existing KSP entry uses ksp:2.3.6-x but Kotlin is 2.3.20.
       The KSP version prefix MUST match the Kotlin version prefix or Room and Hilt code
       generation breaks with the K2 compiler.

       In the plugins {} block, find the KSP entry (it looks like):
         id("com.google.devtools.ksp") version "2.3.6-1.0.22"  (or similar 2.3.6-* value)

       Replace the version with the 2.3.20 series. Check https://github.com/google/ksp/releases
       for the exact patch suffix for Kotlin 2.3.20. As of March 2026 the expected format is:
         id("com.google.devtools.ksp") version "2.3.20-1.0.25"  (verify exact patch suffix)

       If the KSP version already starts with "2.3.20", leave it unchanged.

    1. ADD the kotlinx.serialization plugin to the plugins {} block (after the existing plugins):
       id("org.jetbrains.kotlin.plugin.serialization") version "2.3.20"

    2. ADD buildFeatures block inside android {} (after the existing testOptions/sourceSets blocks):
       buildFeatures {
           buildConfig = true
       }

    3. ADD BuildConfig fields and manifest placeholder inside android {} defaultConfig {} block
       (after testInstrumentationRunner line):
       val localProps = java.util.Properties()
       val localPropsFile = rootProject.file("local.properties")
       if (localPropsFile.exists()) localProps.load(localPropsFile.inputStream())
       buildConfigField("String", "YOUTUBE_API_KEY",
           "\"${localProps.getProperty("YOUTUBE_API_KEY", "")}\"")
       buildConfigField("String", "GOOGLE_CLIENT_ID",
           "\"${localProps.getProperty("GOOGLE_CLIENT_ID", "")}\"")
       manifestPlaceholders["appAuthRedirectScheme"] = "com.musicali.app"

    4. ADD Phase 3 dependencies to the dependencies {} block (after the existing OkHttp/Jsoup lines):

       // Auth — AppAuth PKCE + EncryptedSharedPreferences
       implementation("net.openid:appauth:0.11.1")
       implementation("androidx.security:security-crypto:1.1.0")

       // Credential Manager (Google account picker)
       implementation("androidx.credentials:credentials:1.5.0")
       implementation("androidx.credentials:credentials-play-services-auth:1.5.0")
       implementation("com.google.android.libraries.identity.googleid:googleid:1.0.0")

       // Retrofit 3 BOM + converter (YouTube API client)
       val retrofitBom = platform("com.squareup.retrofit2:retrofit-bom:3.0.0")
       implementation(retrofitBom)
       implementation("com.squareup.retrofit2:retrofit")
       implementation("com.squareup.retrofit2:converter-kotlinx-serialization")

       // kotlinx.serialization JSON (for Retrofit converter + YouTube response models)
       implementation("org.jetbrains.kotlinx:kotlinx-serialization-json:1.7.3")

    5. ADD OkHttp MockWebServer to testImplementation block (for AuthInterceptor test):
       testImplementation("com.squareup.okhttp3:mockwebserver:4.12.0")

    CRITICAL: Do NOT use KAPT. Do NOT add the Google API Java Client Library. Do NOT add any Spotify dependency.
    CRITICAL: The kotlinx.serialization plugin version MUST match the Kotlin version prefix (2.3.20). Do not use a different version.
    CRITICAL: The KSP plugin version prefix MUST be 2.3.20 to match Kotlin. Mismatched KSP breaks Room and Hilt with the K2 compiler.
  </action>
  <verify>
    <automated>cd /Users/alex.maksimchuk/claude/projects/MusicAli && ./gradlew :app:assembleDebug 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - app/build.gradle.kts KSP plugin version starts with "2.3.20" (not "2.3.6")
    - app/build.gradle.kts contains: id("org.jetbrains.kotlin.plugin.serialization") version "2.3.20"
    - app/build.gradle.kts contains: buildConfig = true
    - app/build.gradle.kts contains: buildConfigField("String", "YOUTUBE_API_KEY"
    - app/build.gradle.kts contains: buildConfigField("String", "GOOGLE_CLIENT_ID"
    - app/build.gradle.kts contains: manifestPlaceholders["appAuthRedirectScheme"] = "com.musicali.app"
    - app/build.gradle.kts contains: net.openid:appauth:0.11.1
    - app/build.gradle.kts contains: androidx.security:security-crypto:1.1.0
    - app/build.gradle.kts contains: androidx.credentials:credentials:1.5.0
    - app/build.gradle.kts contains: retrofit-bom:3.0.0
    - app/build.gradle.kts contains: kotlinx-serialization-json:1.7.3
    - app/build.gradle.kts contains: mockwebserver:4.12.0
    - ./gradlew :app:assembleDebug exits 0 (BUILD SUCCESSFUL)
  </acceptance_criteria>
  <done>Project compiles cleanly with all Phase 3 dependencies. KSP version aligned to Kotlin 2.3.20 prefix. BuildConfig.YOUTUBE_API_KEY and BuildConfig.GOOGLE_CLIENT_ID are generated. The appAuthRedirectScheme placeholder is set to com.musicali.app.</done>
</task>

</tasks>

<verification>
After both tasks:
- `./gradlew :app:assembleDebug` exits 0
- `grep "com.google.devtools.ksp" app/build.gradle.kts` returns a version starting with 2.3.20
- `grep -r "YOUTUBE_API_KEY" app/build/generated` finds a BuildConfig.java with the field
- `grep "appauth" app/build.gradle.kts` returns net.openid:appauth:0.11.1
</verification>

<success_criteria>
- Google Cloud Console has YouTube Data API v3 enabled
- local.properties has YOUTUBE_API_KEY and GOOGLE_CLIENT_ID (non-empty values)
- app/build.gradle.kts has all Phase 3 dependencies and BuildConfig fields
- KSP plugin version prefix is 2.3.20 (matching Kotlin)
- Project builds without error: ./gradlew :app:assembleDebug → BUILD SUCCESSFUL
</success_criteria>

<output>
After completion, create `.planning/phases/03-auth-and-youtube-integration/03-01-SUMMARY.md`
</output>
